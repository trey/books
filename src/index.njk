---
layout: layouts/base.njk
title: Books!
description: Search for books at your local library (and other places)
---

<h1>{{ title }}</h1>

<ul>
     {% for item in sources.items %}
     <li>
         {% if item.url %}
         <a href="{{ item.url }}">{{ item.name }}</a>
         {% else %}
         {{ item.name }}
         {% endif %}
         <form action="{{ item.action }}" method="{{ item.method | default('get') }}">
             <input type="text" name="{{ item.field | default('q') }}" />
             <button>Search</button>
         </form>
     </li>
     {% endfor %}
</ul>
